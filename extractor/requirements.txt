# extractor/requirements.txt — cr-report 전용 기사 추출 서비스의 런타임 의존성
#
# 이 서비스는 기사 URL을 받아 제목·본문·메타를 돌려주는 일만 한다. 분석·저장·
# 리포트 생성을 하지 않으므로 anthropic·openai·supabase·json_repair·PDF 관련
# 패키지는 넣지 않는다. 독립성 테스트를 통과시키려고 이 패키지들을 다시
# 설치하거나 빈 core 모듈을 만들지 않는다.
#
# 버전 결정 근거
#   직접 의존성 4개(fastapi·uvicorn·requests·beautifulsoup4)는 cr-check 운영
#   배포의 빌드 로그에서 읽은 실제 설치 버전으로 고정했다. Python 3.11 격리
#   환경에서 그 4개만 설치했을 때 전이 의존성도 같은 버전으로 해결되는 것을
#   확인하고, 그 결과를 아래에 그대로 옮겼다. 전역 pip freeze 를 복사한 것이
#   아니라 깨끗한 런타임 환경에서 얻은 목록이다.
#
#   같은 버전이라고 해서 원본 컨테이너와 바이트 단위로 동일한 이미지가 되는
#   것은 아니다. 원본은 분석용 패키지를 함께 설치하므로 설치 집합이 다르다.
#
# 갱신할 때: 이 파일만 고치고 격리 환경에 다시 설치해 extractor/tests 전체가
# 통과하는지 확인한다. 새 lock 관리 도구를 도입하지 않는다.

# --- 직접 의존성 ---
fastapi==0.119.1
uvicorn==0.52.4
requests==2.34.2
beautifulsoup4==4.15.0

# --- 위 4개가 끌어오는 전이 의존성 (같은 격리 환경에서 확인한 값) ---
annotated-types==0.8.0
anyio==4.15.1
certifi==2026.7.22
charset-normalizer==3.5.1
click==8.5.0
h11==0.16.0
idna==3.19
pydantic==2.13.5
pydantic_core==2.46.5
soupsieve==2.9.2
starlette==0.48.0
typing-inspection==0.4.4
typing_extensions==4.16.0
urllib3==2.7.0
