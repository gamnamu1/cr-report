/**
 * 조립 문자열의 postamble — 기사 본문 뒤에 오는 참고 자료.
 *
 * PR2 에서는 자리표시다. 네 섹션의 제목과 순서만 확정한다(지시서 §3-3).
 * 서버에서만 import 한다.
 */
export const KIT_POSTAMBLE = `[출력 양식]
(PR3에서 집필)

[핵심 규범 원문]
(PR3에서 집필)

[문제적 보도관행 지도]
(PR3에서 집필)

[예시 리포트]
(PR3에서 집필)`;
