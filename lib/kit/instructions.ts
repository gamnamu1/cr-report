/**
 * 조립 문자열의 preamble — 기사 정보보다 앞에 오는 지시문.
 *
 * PR2 에서는 자리표시다. 본문 집필은 PR3 이며, 여기서 확정하는 것은
 * 섹션 제목과 조립 순서다(지시서 §3-3 · §3-4).
 * 서버에서만 import 한다.
 */
export const KIT_PREAMBLE = `[CR 기사 비평 요청]
(PR3에서 집필)`;
